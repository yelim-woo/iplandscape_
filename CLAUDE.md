# iplandscape

Next.js 기반 IP Landscape 분석 워크벤치 + 특허 분석 하네스.

## 하네스: IP Landscape 분석

**목표:** 특허 DB 엑셀(원본/유효) → 유효데이터 정제 → 정량분석(차트+해석) → 경쟁사 상위기업 상세 → 정성분석(기술흐름도·O/S Matrix·특허맵 등고선) → 핵심특허 선별 → 통합 PDF 보고서까지 end-to-end 자동화.

**트리거:** 특허 데이터로 전체 IP Landscape 분석/보고서를 요청하면 `ipl-pipeline` 오케스트레이터 스킬을 사용하라. 단일 분석(경쟁사만, 등고선만, 핵심특허만 등)이면 해당 개별 스킬(`ipl-competitor` / `ipl-contour-map` / `ipl-core-patent` / `ipl-report`)을 직접 쓴다. 데이터 정제·정량·정성 기본 단계는 기존 `patent-filter` / `patent-trend` / `patent-quality`를 재사용한다. 단순 질문은 직접 응답.

**에이전트(.claude/agents/):** data-curator · quant-analyst · competitor-analyst · qualitative-analyst · core-patent-finder · market-analyst(웹리서치 기반 기술·시장 환경분석) · report-integrator (모두 opus, 서브 에이전트 하이브리드)

**워크스페이스 규약:** 모든 중간 산출물은 `<데이터폴더>/_ipl/{quant,competitor,qualitative,core,market}/`에 보존, 최종 `IP_Landscape_Report.pdf`만 폴더 루트. 통합 빌더는 `_ipl/manifest.json`을 소비.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-05 | 초기 구성 — 에이전트6 + 신규스킬4(ipl-competitor/contour-map/core-patent/report) + 오케스트레이터(ipl-pipeline) | 전체 | 특허 엑셀→IP Landscape PDF end-to-end 파이프라인 요청 |
| 2026-06-05 | 대용량 스트리밍 1차필터 추가 (`_filter_stage1_streaming.py`) | patent-filter | 34만건/8.7GB WIPS raw에서 기존 stage1의 메모리 OOM 회피 (파일별 스트리밍+벡터화+필요컬럼만) |
| 2026-06-05 | Lens CSV→WIPS xlsx 변환·정제기 추가 (`_lens_to_wips.py`) | patent-data | Lens.org export(영문 CSV)를 패밀리 중복제거+최근N년 정제 후 기존 스킬 호환 스키마로 변환. EV/배터리 5만건 보고서 생성에 사용 |
| 2026-06-05 | PDF 양식 전면 개편 — 장 번호박스+이중가로줄, 그림 우선 배치(<그림 N-M> 캡션), ○불릿, KeepTogether 블록 무결성, 한글 볼드 등록 | ipl-report/_build_pdf.py | 사용자 제공 특허분석 보고서 양식 일치 + "제목/그림이 페이지 경계서 끊김" 문제 해결 |
| 2026-06-05 | 경쟁사·정성·핵심특허 장도 정량 양식으로 통일 — 번호 매긴 섹션(그림 우선/캡션), 경쟁사 그림 1·2·3 + 프로파일 4섹션, 핵심특허 표(<표 5-1>)+근거 2섹션 | ipl-report/_build_pdf.py | 전 장 페이지 구성 일관성 |
| 2026-06-05 | 프리미엄 디자인 적용 — 표지(네이비 그라데이션+점선테두리+대형 영문타이틀+네트워크 그래픽, canvas), 전 페이지 상단 룰+아이콘 장식과 페이지번호 네이비박스 푸터(draw_cover/draw_deco), 제N장 태그, 파란 탭 섹션제목(sec_heading), 네이비 표 스타일(navy_table) | ipl-report/_build_pdf.py | KIPO IP Report 스타일 고급 보고서 디자인 요청 |
| 2026-06-05 | 장 구분 페이지 추가 — 각 장(1~5) 시작 전 네이비 패널 divider 페이지(CHAPTER NN/제N장/대형 제목/숫자 워터마크) 삽입(ChapterDivider/chapter_div), 내용 페이지의 중복 장 헤더 제거 | ipl-report/_build_pdf.py | 장 구분 시 전용 페이지 추가 요청 |
| 2026-06-05 | 정량 해석 강화 — 차트당 6~8불릿, '수치 관찰 1~2 + AI 해석(동인·함의·비교·전략 시사점·전망) 4~6'의 다층 해석 비율 명시 | agents/quant-analyst.md | 정량 해석에 AI 해석 비율·분량 확대 요청 |
| 2026-06-05 | PDF 디자인 KIST 스타일로 전면 전환 — 차콜+코랄 팔레트, 우측 세로밴드 표지, 목차 페이지(2-pass 정확한 페이지번호+작성안내 박스), 풀블리드 차콜 장 밴드(CHAPTER NN), 코랄 섹션바/▪불릿/페이지박스 푸터(장명 표시), KIST 표(코랄 라벨열). 네이비 divider 페이지는 인라인 장 밴드로 대체 | ipl-report/_build_pdf.py | KIST IP 랜드스케이프 양식 느낌 요청 |
| 2026-06-08 | 해석 가독성·심화 — 해석 라벨형 정의리스트({l,t}) 렌더, 정량 '국가별 등록현황' 차트 제외, 경쟁사 차트별 해석+대표특허표(피인용·패밀리)+라벨 프로파일, 정성 등고선 2종(IPC/기술주제 키워드), 핵심특허 표 점수 제거+근거 가독성 | ipl-report·ipl-contour-map·ipl-competitor·agents | 섹션별 해석 가독성·상세화·등고선 2종 요청 |
| 2026-06-08 | 디자인 전면 교체 — KIST(차콜+코랄·풀블리드 밴드) → 미니멀 에디토리얼(모노톤 잉크/그레이, 얇은 헤어라인, 넉넉한 여백). 표지(상하 헤어라인·좌측정렬), 장 헤더(연회색 큰 번호+CHAPTER+제목+풀폭 룰), 가로선만 표, 회색 라벨 해석, '— N —' 푸터·러닝헤더 | ipl-report/_build_pdf.py | "심플하지만 고급스러운" 디자인 요청 |
| 2026-06-08 | 경쟁사 장 페이지 구분 개선 — 대표특허 표(제목+표+캡션) KeepTogether로 쪼개짐 방지 + 새 페이지 시작, 상세 프로파일도 새 페이지 시작. 차트3→표→프로파일로 논리 분리 | ipl-report/_build_pdf.py | 경쟁사 페이지 구분 요청 |
| 2026-06-08 | 경쟁사 심화 — 상위 출원인 기술흐름도(IPC 구간변화)+O/S Matrix(과제×수단, 신규 _competitor_quality.py) 차트 추가, 기업별 핵심특허 요지·권리범위·대응전략 심층 프로파일, 대표특허에 요약 포함 | ipl-competitor·ipl-report·competitor-analyst | 기업별 상세 특허분석·기술흐름도·OS matrix 요청 |
| 2026-06-08 | KIPRIS 입력 지원 — 강건 엑셀 리더(손상 스타일시트 calamine 폴백 + 헤더행 자동탐지)를 _ipl_io·patent-filter·patent-trend에 반영, KIPRIS 출원인 말미 (주소) 제거, 특허번호 float→정수문자열 정리(clean_id) | _ipl_io·patent-filter·patent-trend·ipl-competitor·ipl-core-patent | KIPRIS xlsx 입력 작동 요청 |
| 2026-06-08 | 커밋 전 코드 점검·하드닝 — .gitignore 화이트리스트에 하네스(agents·ipl스킬·patent-filter/quality/data·CLAUDE.md) 포함(이전엔 누락), 빌드캐시(__pycache__/.pyc/.tsbuildinfo) 제외; 방어코드(손상이미지 가드·None 방어·빈데이터/동질데이터 가드·필수컬럼 .get()·파일핸들 with), 등고선 2모드 파일명 문서 일치화 | .gitignore·ipl-report·ipl-contour-map·_lens_to_wips·patent-trend·docs | 깃 커밋 전 전체 코드 리뷰 요청 |
| 2026-06-08 | Phase 2 실행방식 변경 — `run_in_background: true` 폐기(백그라운드는 권한 프롬프트 자동거부로 조용히 실패). 4개 분석 에이전트를 한 메시지에 함께 호출해 포그라운드 동시 실행, 막히면 순차 폴백 | ipl-pipeline/SKILL.md | 타 PC 테스트에서 백그라운드 권한 자동거부로 실패 → 포그라운드로 완료한 피드백 |
| 2026-06-08 | 공유 권한 설정 추가 — `.claude/settings.json`에 안전 작업 패밀리 자동허용(Read/Write/Edit/Glob/Grep, Bash: python·pip·mkdir·cp·cd·ls·cat·grep·find 등), 위험 작업(rm·mv·git push·curl·sudo 등)은 계속 프롬프트, taskkill 차단. settings.json은 공유(git 포함), settings.local.json은 개인용(제외) | .claude/settings.json·.gitignore | 하네스 실행 중 반복 allow 프롬프트 과다 → 안전 항목 자동허용 요청 |
| 2026-06-09 | patent-search에 **KIPRIS** DB 추가 — `<kipris>` 토큰으로 선택. KIPRIS 스마트검색 연산자 조사 반영(AND=`*`/OR=`+`/NOT=`*!`, 포함연산 `(...)<in>(TL,AB)`, 다단어 `"구문"`, IPC `(...)<in>IPC`, 날짜 `[YYYYMMDD~YYYYMMDD]<in>AD`, 관할절 없음=KR). SKILL.md 🅲섹션 + db-config(Db타입·라벨·KIPRIS URL)·stage1-rebuild(연산자/필드/IPC/필터 db분기)·stage1 타입·db-badge CSS | patent-search/SKILL.md·dashboard(db-config·stage1-rebuild·prompts/stage1·globals.css) | WIPS·Lens만 있던 검색식 설계에 KIPRIS 연산자 추가 요청 |
| 2026-06-09 | KIPRIS 절단기호 수정 — 텍스트 키워드 후방절단 `?`(및 `*`)를 **제거**(KIPRIS는 자동절단검색 기본, `?`는 번호 전용→텍스트엔 "특수문자" 오류). `formatKeyword` kipris 분기가 `*`/`?` 제거+다단어/하이픈/슬래시 인용, SKILL.md 절단 규칙·예시 교정 | patent-search/SKILL.md·dashboard/stage1-rebuild | 실제 KIPRIS 검색 시 `?`가 특수문자로 거부됨 피드백 |
| 2026-06-09 | KIPRIS 검색식 전면 키워드-불리언화 — 자유검색창이 `<in>`·`(TL,AB)` 콤마·`[날짜]`·인라인 IPC를 모두 거부(특수문자 오류) 확인. fieldBlock kipris를 `(terms)`로(필드지정 제거), rebuildPrecise kipris는 IPC/날짜 미부가(=basic), 허용연산자 `* + *! ^ "" ()`만. SKILL.md 🅲 재작성(IPC·기간은 상세검색 폼 안내), 복합소재 JSON 36개 query를 키워드불리언으로 변환+searchTips 교체 | patent-search/SKILL.md·dashboard/stage1-rebuild·복합소재.json | KIPRIS 검색 시 `<` `[` `]` `,` `.` 까지 특수문자 거부됨 피드백 |
| 2026-06-12 | KIPRIS 출원일 기간 연산자 복원 — `AD=[YYYYMMDD~YYYYMMDD]`는 스마트검색에서 허용됨 확인. precise 끝에 `*AD=[(현재연도−10)0101~오늘]`(최근 10년, 매 실행 자동산정) 부착. SKILL.md 🅲 규칙6 신설·구조/예시·searchTips 갱신, stage1-rebuild: kiprisAdDateFilter() 추가·FILTER_MATCHERS.kipris가 `AD=[`인식·rebuildPrecise가 원본 보존 또는 자동 부착 | patent-search/SKILL.md·dashboard/stage1-rebuild | KIPRIS 검색식에 현재연도 기준 최근 10년 출원일 필터를 검색식 자체에 붙여달라는 요청(AD=[시작~현재일자] 동작 확인) |
| 2026-06-12 | KIPRIS AD 누락 방지 하드닝 — 타 PC 테스트서 설계된 precise에 AD가 안 붙는 문제. 생성은 Claude가 SKILL.md로 직접 함을 확인(대시보드엔 generate API 없음). ①SKILL.md 핵심규칙에 🅺 강제규칙(소분류 queries.precise+unifiedQuery.precise 모두 `*AD=[..]` 필수+자가검증) ②대시보드 로드 시 `ensureKiprisDate()`로 모든 precise(트리+unified)에 기간 결정론적 보장(AI 누락해도 화면·[열기]·복사 모두 적용) | patent-search/SKILL.md·dashboard/stage1-rebuild·stage1/page.tsx | zip 타 PC 테스트서 KIPRIS 설계 시 AD 미반영 피드백 |
| 2026-06-30 | 정성분석 장 빈 divider 페이지 제거 — 정성 등고선 블록만 [제목+그림+캡션+해석 전체]를 한 KeepTogether로 묶어 장 헤더 다음에 안 들어가 다음 페이지로 밀리던 문제(장 헤더만 있는 빈 페이지 발생). 정량·경쟁사처럼 **[제목+그림+캡션]만 KeepTogether, 해석은 흐름** 처리로 변경 → 장 헤더+첫 그림이 같은 페이지에 이어짐 | ipl-report/_build_pdf.py | 정성분석 장 헤더 페이지가 비고 다음 페이지부터 시작 → 정량/경쟁사처럼 바로 이어지게 요청 |
| 2026-06-30 | 키워드 등고선 전처리 강화 — 기술주제(키워드) 등고선에 method·system·composition 등 특허 상투어와 JPO/INPIT 영문초록 푸터(jpo·inpit·copyright·drawing)가 키워드로 잡히던 문제. `_contour.py`: ①STOP에 특허/명세서 보일러플레이트(composition·agent·compound·extraction 등) + JP푸터 + 잔여상투어 대폭 추가 ②복수→단수 정규화 `_canon()`(methods→method 등 단수 STOP로 차단·집계 통합), 원형·정규형 둘 다 STOP 체크 ③문서빈도 상한 40%→25%(거의 모든 특허에 나오는 비변별 용어 자동 제거)·하한 1.5%. 결과 키워드가 herbal·botanical·cosmetic·skin·acid·active·food·pharmaceutical 등 변별력 있는 도메인 주제로 정리. 매니페스트 contour_tech 해석·핫스팟/공백도 새 키워드로 갱신 | ipl-contour-map/_contour.py·manifest | "키워드 등고선에 method·system 등 당연 키워드만 나옴 — 불용어·특허특화 제거 필요" 요청 |
| 2026-06-30 | 경쟁사 장 단순 O/S 히트맵 제거 — O/S Matrix를 정성분석 장의 구간형(공백 강조) 하나로 일원화. 빌더 양쪽 경쟁사 차트 목록에서 `osmatrix` 항목 제외(점유율/추이/IPC/기술흐름도는 유지). competitor의 comp_osmatrix.png/comp_quality는 산출은 되나 보고서 미렌더 | ipl-report(_build_pdf·_build_hwpx) | O/S 매트릭스 2개 중복 → 경쟁사 장 기존것 제거 요청 |
| 2026-06-30 | O/S 정의 정정(Object=목적, 효능 아님) — Object=목적(해결과제, "무엇을 해결")·Solution=수단(해결수단, "무엇으로 해결")로 용어 통일. 잘못 표기됐던 '효능(Object)'·표헤더 '효능×수단' → '목적/해결과제'·'목적×수단'으로 수정(빌더·매니페스트 whitespace_strategy 문구·매트릭스 축 라벨 'Object(목적)/Solution(수단)'). 데이터에 별도 과제/수단 컬럼 부재 시 명칭+요약에서 목적키워드/수단키워드로 분류함을 qualitative-analyst에 명시 | ipl-report(_build_pdf·_build_hwpx)·ipl-contour-map/_os_matrix.py·agents/qualitative-analyst·manifest | "Object는 효과가 아니라 목적, Solution은 수단" 정의 정정 |
| 2026-06-30 | 구간형 O/S Matrix + 공백기술·전략(보고서 핵심목적=White Space 발굴) — 신규 `ipl-contour-map/scripts/_os_matrix.py`: 전 유효특허를 Object(해결과제)×Solution(해결수단) 분류·출원 3구간으로 쪼개 **대각선(같은 구간)에만 카운트**, 조합을 **성숙(핵심·레드오션, 차콜)/성장(초록)/신규(주황)/공백(파랑·기회점수 순위①②③)** 색분류(레퍼런스 이미지 형태). 공백 기회점수=Object·Solution 활성도 기하평균. 빌더에 `LandscapeImage`(이미지 90°회전 가로 전용 페이지) 추가해 매트릭스를 가로 페이지로, 이어 「공백기술 & 진입 전략」 표(순위·조합·근거·전략)+등고선 공백 종합 섹션 렌더(`whitespace_strategy` 최상위 키). qualitative-analyst가 `_os_matrix.py` 실행+전략 작성, manifest `qualitative.os_matrix`/`whitespace_strategy` 조립. HWPX도 전략 표 반영(가로회전 미지원→정상 도표). **`landscape` 기본=세로(6×6은 페이지 폭에 맞춤), 8×8+ 등 매우 넓을 때만 true로 가로 회전** | ipl-contour-map/_os_matrix.py·ipl-report(_build_pdf:LandscapeImage·_build_hwpx·SKILL)·agents(qualitative-analyst·report-integrator)·CLAUDE.md | "공백기술·전략이 안 드러난다 — O/S 매트릭스를 첨부 이미지(구간형) 형태로 + 공백 강조·전략 종합" 요청 |
| 2026-06-30 | 핵심 구절 볼드 보완(현재 보고서) — 기존 정량·경쟁사·정성·핵심특허·요약 해석은 `**` 마커가 없어 자동 볼드(수치)만 걸렸던 것을, manifest 후처리로 각 불릿의 핵심 결론 구절에 `**...**` 165곳 마킹(market 17곳 불변, 분석 재실행 없이 재빌드). 볼드 기준 자체(=`rich()`: `**강조**`+수치 자동 볼드)는 그대로이며, 다음 파이프라인 실행부터는 에이전트가 직접 `**핵심구절**`을 달아 전 장 구절 볼드가 자동 적용됨 | manifest(후처리)·해당 보고서 PDF/HWPX 재빌드 | "수치에만 볼드된다 — 중요 구절도 볼드" 피드백 |
| 2026-06-30 | 본문 띄어쓰기 벌어짐 수정 — 양쪽정렬(TA_JUSTIFY)이 한글 어절 공백을 늘려 '칸이 여러 개' 벌어져 보이던 문제를, 본문/불릿 스타일(body·obul·ibul·exbul·small)을 **왼쪽정렬(TA_LEFT)**로 전환해 해결. 추가로 `esc()`(PDF·HWPX)에서 연속 공백(탭·NBSP·전각 포함) → 한 칸 정규화 | ipl-report(_build_pdf·_build_hwpx) | 생성 문장 중간 띄어쓰기 여러 칸 벌어짐 해결 요청 |
| 2026-06-30 | 보고서 4종 개선 — **①부록 제거**(CHAPTERS·양 빌더에서 Appendix 장 삭제, manifest appendix 미사용) **②차트 디자인 고급화**: 엑셀풍 탈피 — 절제된 에디토리얼 팔레트(네이비/브릭/틸/플럼/골드)·흰 배경·옅은 실선 그리드(점선 제거)·눈금마크 제거·파이 그림자 제거(stage_a V2팔레트+_ipl_io IPL_PALETTE/COUNTRY_COLOR 동시 교체, 4사본 동기화). **변경 직전 백업: `.claude/_chart_design_backup_20260630/`**(되돌리기용) **③본문 강조**: `rich()` 신설 — `**강조**` 마크다운→볼드 + 핵심 수치(%·금액·건수·배수·CAGR) 자동 볼드(obullets/interp_block/core reason 적용). HWPX는 인라인 볼드 생략·`**`만 제거. 5개 분석/시장 에이전트에 `**핵심구절**` 작성 지침 추가 **④기술·시장 분석 출처**: 문장별 `{t,refs}`→본문 끝 `[n]` 윗첨자, 출처는 맨 뒤 '참고문헌(References)' 장에 번호순 정리(market 스키마 sources→references+refs, 빌더·market-analyst·SKILL 갱신) | ipl-report(_build_pdf·_build_hwpx·_ipl_io·SKILL)·patent-trend(_gen_report_stage_a)·ipl-competitor·ipl-contour-map·agents(market-analyst·quant·competitor·qualitative·core)·_chart_design_backup | 부록 제거·차트 고급화(백업)·중요포인트 볼드·시장분석 출처 각주화 요청 |
| 2026-06-30 | 불릿 마커 변경 — 문단 앞 대시(—) → 네모박스(■, 회색 소형). PDF `obullets`(전 장 공통)·HWPX `p_bullet`/`p_label_bullet`(○→■) 양쪽 적용. Malgun/한글 폰트에 글리프 존재 확인 | ipl-report(_build_pdf·_build_hwpx) | 문단 앞 마커를 네모박스로 변경 요청 |
| 2026-06-30 | 「기술·시장 분석」 장 카드형 디자인 — 밋밋한 불릿 → 섹션별 카드(연회색 헤더 밴드+우측 영문 태그 TECHNOLOGY/DOMESTIC/GLOBAL+하단 잉크 룰)와 본문 좌측 액센트 룰(`band_heading`·`panel_body` 헬퍼 신설), 도입 리드 문장 + 하단 하이라인+굵은 '주요 출처' 푸터. 각 카드는 KeepTogether(헤더+본문)로 고아 방지. 미니멀 모노톤 톤 유지 | ipl-report/_build_pdf.py | 기술·시장 분석 장 디자인 개선 요청 |
| 2026-06-30 | 「기술·시장 분석」 장 신설(입력 데이터와 별개의 주제 기반 환경분석) — 신규 에이전트 `market-analyst`(웹 리서치로 기술 이해 + 환경분석 국내/국외 최근 소식 요약 → `_ipl/market/market.json`) 추가, ipl-pipeline Phase 2 팬아웃에 5번째로 합류(유효데이터 불요·주제만 필요·독립 병렬). 빌더 양쪽(_build_pdf·_build_hwpx)이 manifest `market` 키를 Executive Summary 다음·분석 개요 앞에 **번호 없는 장**(기술 이해→환경 국내→환경 국외→출처)으로 렌더. ipl-report SKILL 스키마·report-integrator·CHAPTERS(부록 인덱스 -1로 보정) 갱신. market 비면 장 자동 생략 | agents/market-analyst·ipl-pipeline·ipl-report(_build_pdf·_build_hwpx·SKILL)·report-integrator·CLAUDE.md | 입력 특허데이터와 별개로 주제 맞춤 시장분석(기술 이해+국내/국외 최근 동향)을 분석 개요 앞에 넣어달라는 요청 |
| 2026-06-30 | 보고서 레이아웃·가독성 개선 — ①해석 반복 라벨(현황/동인/비교/함의/유의/전략/전망) 제거: `interp_block(labels=)` 추가, 정량·경쟁사차트·정성 해석은 라벨 없이 가독성 불릿(ibul 스타일, 항목간 여백↑)으로, 경쟁사 기업 프로파일의 도메인 라벨(기술포커스/대응전략 등)은 유지 ②그래프+해석이 페이지 경계서 쪼개지던 문제 → `figure_block`을 단일 KeepTogether로 복원(차트+캡션+해석 한 단위)하고 라벨 제거로 압축해 한 페이지에 안착, 차트 max_h 8.6cm로 축소(장헤더 고아 방지와 양립) ③표지 제목 항상 1줄 — 본문폭에 맞게 폰트 자동 축소(줄바꿈 방지) ④Executive Summary 가독성 — 리드인 문장 + exbul 스타일(행간/여백↑). HWPX도 동일하게 차트/정성 해석 라벨 제거+요약 리드인 적용 | ipl-report(_build_pdf·_build_hwpx) | 생성 보고서에서 그래프+해석이 한 페이지에 안 들어가고 틀 깨짐·반복 라벨 가독성·표지 제목 줄바꿈·요약 가독성 개선 요청 |
| 2026-06-30 | 차트 톤 통일·가독성(cosmetic 3종) — ⑧경쟁사/정성 차트를 정량(stage_a) 프리미엄 룩으로 통일: 공유 차트헬퍼(`chart_fig`·`style_axes`·`axis_unit`·`mark_incomplete_year`·`IPL_PALETTE`/`IPL_COUNTRY_COLOR`)를 `_ipl_io.py`에 추가(4사본 동기화), `_competitor`·`_competitor_quality`·`_contour`가 `#fafafa` 배경·스파인 제거·은은한 점선그리드·공유 팔레트·차트내 제목 제거(보고서 캡션이 담당)로 렌더 ⑨y축 단위 라벨을 가로 배치(한글 세로쓰기 방지) — stage_a 차트1·4·6·7·8 및 경쟁사 차트 ⑪시계열 마지막 연도(공개 18개월 시차로 미집계)를 회색밴드+'최근(공개 시차)' 주석으로 표시해 급락 착시 방지 — stage_a 차트1·4·7·8 및 경쟁사 추이차트(연도 4개 이상일 때만) | _ipl_io·ipl-competitor(_competitor·_competitor_quality)·ipl-contour-map(_contour)·patent-trend(_gen_report_stage_a) | 정량 vs 경쟁사 차트 스타일 불일치·세로축 라벨 가독성·2026 미공개 구간 급락 착시 개선 요청 |
| 2026-06-30 | 생성 보고서 품질 결함 일괄 하드닝 — ①PDF 본문 `&` 미이스케이프로 `R&D`→`R&D;` 깨짐 수정(`esc()` 전 텍스트 적용) ②데이터개요 표 값 셀 Helvetica 폴백으로 한글('건'·파일명)이 □로 깨지던 것 수정(`mono_table` FONTNAME 기본값=한글폰트) ③정제기준에 로컬 전체경로·깨진 파일명 노출 → `criteria_summary` 우선·없으면 파일명만+`clean_label` ④KR 13자리 무하이픈 특허번호 → `fmt_patent_no`로 `10-등록번호`/`10-연도-일련` 정리(JP/US 보존, 멱등) ⑤대표특허 표 피인용·패밀리 데이터 없으면 컬럼·캡션 자동 생략 ⑥표 긴 텍스트 글자단위 잘림 → `…` 말줄임(`trunc`) ⑦CH02 빈 divider 페이지(장헤더 고아) → `figure_block`이 [제목+그림+캡션]만 KeepTogether하고 해석은 흐름 처리 ⑧표지 작성자 공란 시 '작성' 줄 생략. 공유헬퍼 `fmt_patent_no`/`trunc`/`clean_label`을 `_ipl_io.py` 4사본 동기화 | _ipl_io·ipl-report(_build_pdf·_build_hwpx·SKILL)·ipl-competitor·ipl-core-patent·agents/data-curator | 생성된 IP_Landscape_Report.pdf 검토 후 발견한 렌더/데이터 결함을 스킬·스크립트 차원에서 재발 방지 |
| 2026-07-01 | 1차 법적상태 필터에 '판정 보류(공란) 통과' 도입 — KIPRIS 해외검색 결과처럼 법적상태·등록번호·등록일을 **아예 제공하지 않는** 해외 국가 데이터(CN·US·JP·EP·WO)가 "법적상태 비등록"으로 **전량 탈락**하던 문제. 원인은 등록번호/등록일 유무가 아니라 해외 스키마엔 법적상태 컬럼 자체가 공란인 것. `status_is_blank()` 신설: 법적상태가 NaN·빈문자면 제외하지 않고 통과시켜 2차 의미판정으로 위임(명시적 비등록 상태는 종전대로 제외), 보류 건수를 로그·`_meta.json.status_blank_held`에 기록. 스트리밍판도 (등록번호 없음 ∧ 상태정보 없음)=보류 통과로 동일 적용. 부수 하드닝: `load_folder`가 이전 산출물 `제외로그.xlsx`·`_ipl/`도 입력에서 제외(재실행 시 자기 출력 재흡수 방지), stdout utf-8 고정(cp949 콘솔서 `—` 등 로그 크래시 차단). 실데이터 검증: KIPRIS 3000건(국내500+해외2500)에서 1차 통과 ~200→2489건, 해외 2500 보류 통과 | patent-filter(_filter_stage1·_filter_stage1_streaming·SKILL.md) | "등록 기준 필터가 중국 등 해외 데이터를 등록번호 없다고 전부 제거" → 등록번호가 아니라 해외 법적상태 미제공이 원인임을 실데이터로 규명, 보류 통과로 보존 |
| 2026-07-01 | 정량·경쟁사 장 시작 빈 페이지 제거 — `figure_block` 해석 흐름화 | 정량(2장) 등 장 시작에서 해석 불릿이 많으면(6~8개) [제목+그림+캡션+해석] 전체를 한 KeepTogether로 묶던 `figure_block`이 장 헤더 다음 남은 공간에 못 들어가 **통째로 다음 페이지로 밀리며 장 헤더만 남는 빈 페이지**가 생기던 문제. 정성분석 장이 이미 쓰던 방식대로 **[제목+그림+캡션]만 KeepTogether, 해석(interp_block)은 자연 흐름**으로 변경 → 그림+캡션은 붙고 긴 해석은 페이지 하단 여백을 남기며 다음 장으로 이어짐(앞이 아닌 뒤에 여백). 실검증: 정량 장 시작이 헤더+차트+6불릿 한 페이지 안착, 총 41→40p(빈 페이지 회수). 경쟁사 장도 동일 견고화. HWPX는 무관(별도 빌더) | ipl-report/_build_pdf.py | 생성 PDF에서 정량 장(9p)이 헤더만 있고 백지 → 해석 많을 때 뒤에 공간 남기고 넘어가게 요청 |
| 2026-07-01 | 출원인 표기 통합 일반화 + 표지 연구주제 자동맞춤 | ①경쟁사 `norm_applicant`의 iRobot 하드코딩(`{"irobot":"iRobot Corporation"}`) 제거 → 범용 `canonicalize_app_series()` 신설(대소문자·공백만 다른 동일 출원인을 casefold 키로 묶어 최빈 표기로 통합, 동률 시 대소문자 혼용=브랜드 원표기 우선). 특정 기업 하드코딩 없이 'iRobot Corporation'↔'IROBOT CORPORATION'·'ABB'↔'abb' 등 자동 병합(_competitor·_competitor_quality 양쪽). ②PDF 표지 '연구주제 ·' 라벨이 길면 A4 본문폭을 넘던 문제 → 폰트 11→8.5 자동 축소, 그래도 넘치면 `_wrap_width()`로 최대 2줄 문자단위 줄바꿈(제목 위로 쌓아 겹침 방지)+말줄임. 61자 로봇 주제로 검증(2줄 안착) | ipl-competitor(_competitor·_competitor_quality)·ipl-report(_build_pdf) | iRobot 하드코딩을 범용 정규화로 개선 요청 + 표지 연구주제 줄이 A4 폭 초과 |
| 2026-07-01 | 국내/해외 스키마 정합 — 갈라진 개념 컬럼 통합(`unify_columns`) | KIPRIS 국내+해외 익스포트를 한 폴더에 넣으면 같은 개념이 이름만 미세하게 달라(`발명의명칭`↔`발명의 명칭`, `IPC분류`↔`IPC`) 컬럼이 갈라지고, 다운스트림 `auto_map_columns`가 먼저 매핑된 쪽만 취해 **해외행 IPC·명칭이 정량(IPC 분포)·정성(등고선·기술흐름) 차트에서 통째로 누락**되던 문제. `_apply_verdicts.py`에 `unify_columns()` 추가: 개념 그룹(명칭·IPC·CPC·요약·출원인·출원일·등록번호·국가)별로 대표 컬럼에 빈칸을 변형 컬럼값으로 채우고 중복 제거, 국가가 비고 출원번호가 숫자만이면 `KR` 보정. 유효데이터 조립 직전 1회 적용(원본 단일 스키마=변화 없음). 실검증: KIPRIS 2489건에서 발명의명칭 160→2489, IPC분류 160→2489, 국가 2329→2489(KR160/CN499/JP496/WO467/US437/EP430), auto_map 연쇄로 Current IPC Main·국가코드 전량 채움 | patent-filter(_apply_verdicts·SKILL.md) | "정량차트에 해외 데이터가 반영 안 됨 — 국내/해외 컬럼 달라도 텍스트로 맞춰 진행" 요청 |
| 2026-06-18 | 통합 보고서 HWPX 동시 출력 추가 — PDF 빌더와 **동일한 manifest.json**을 소비하는 한글 빌더(`_build_hwpx.py`) 신설. patent-dashboard `양식.hwpx`를 ipl-report/assets에 벤더링해 템플릿으로 사용(차트 PNG를 BinData 임베드+content.hpf 갱신), 핵심특허·경쟁사 표는 header.xml에 borderFill 8(헤더 음영)·9(격자) 주입해 렌더. 표지+요약+5장 구조를 한글 양식(장헤더 표·개조식 불릿)으로 재현(PDF 프리미엄 비주얼은 1:1 미복제). report-integrator가 PDF·HWPX 둘 다 빌드(독립 산출물, 한쪽 실패해도 다른쪽 산출), `IP_Landscape_Report.{pdf,hwpx}` 동시 출력 | ipl-report(_build_hwpx.py·assets/양식.hwpx·SKILL.md)·ipl-pipeline·agents/report-integrator·CLAUDE.md | "보고서를 PDF뿐 아니라 한글(hwpx)로도 같이 만들어달라" 요청 |
